# 原型交互回归记录

> 日期：2026-07-15  
> 结果：通过

## 执行方式

```powershell
node prototype/regression.test.js
```

预期输出：

```text
REGRESSION_OK
```

## 覆盖范围

| 模块 | 回归内容 | 结果 |
|---|---|---|
| 热点 | 关键词过滤、无结果、部分平台失败提示 | 通过 |
| 素材 | 搜索、当前结果全选与取消 | 通过 |
| 创作 | 空白项目、母稿生成、平台生成、状态计算 | 通过 |
| 异常 | 单平台失败、其他平台不受影响、单独重试 | 通过 |
| 图片 | 初始化、新增提示词、刷新、排序、删除 | 通过 |
| 版本 | 首次保存、连续保存、上一版本恢复 | 通过 |
| 分发 | 任务建立、复制状态、全部发布后项目完成 | 通过 |
| 搜索筛选 | 创作、分发和日志的组合条件与无结果状态 | 通过 |
| 设置 | 设置状态与本地存储同步 | 通过 |
| 统计 | 项目、素材、发布任务及 Token 动态统计 | 通过 |

## 静态检查

- `prototype/app.js` 通过 Node.js 语法检查。
- 页面初始化通过最小 DOM 环境运行检查。
- 所有 `data-action` 均存在处理映射。
- 未发现遗留的图片排序、备份选择或字段刷新假交互。

## 说明

该回归验证原型状态变化、渲染结果和核心交互逻辑。正式 Electron 工程仍需使用 Playwright 覆盖真实窗口、文件选择、剪贴板、系统凭据和跨平台快捷键。
